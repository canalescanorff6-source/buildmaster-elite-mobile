# Correção CI e publicação v38.40

- adiciona as notas da versão 38.40 exigidas pelo pré-voo da Google Play;
- fixa a rota inicial autenticada no código-fonte, sem depender do autorreparo do CI;
- atualiza regressões legadas para o cache e a persistência de sessão da v38.40;
- amplia os contratos visuais para reconhecer a versão 38.40;
- sincroniza o workflow de AAB com a versão atual do pacote.
- corrige o tipo interno do modo adaptativo do OCR (`balanced | fast`), que fazia o TypeScript completo encerrar com código 2;
