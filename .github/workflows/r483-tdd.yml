name: R483 TDD Build Simulator

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  r483:
    name: R483 final compatibility
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Baixar código
        uses: actions/checkout@v5
      - name: Configurar Node.js
        uses: actions/setup-node@v5
        with:
          node-version-file: .node-version
          cache: npm
          cache-dependency-path: package-lock.json
      - name: Instalar dependências
        run: npm ci
      - name: R483 oficial
        run: npm run test:r483
      - name: R192 workspace avançado
        run: npm run test:r192
      - name: R189 superfície de resultado
        run: npm run test:r189
      - name: R128 selo final de saída
        run: npm run test:r128
      - name: Compatibilidade gravação Android v31.76
        run: npm run test:v3176
      - name: Compatibilidade vídeo v31.77
        run: npm run test:v3177
