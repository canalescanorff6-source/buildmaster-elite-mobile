BUILDMASTER ELITE TÁTICO v27.26 — CORREÇÃO DO ERRO DO GITHUB ACTIONS

Erro corrigido:
"Manifesto placeholder antigo não pode ser empacotado no APK."

Causa:
O arquivo public/update-manifest.json continuou no repositório antigo. Ao substituir arquivos pelo navegador do GitHub, arquivos ausentes no pacote novo não são apagados automaticamente.

Correção automática aplicada:
- o projeto remove public/update-manifest.json antes do TypeScript;
- remove antes de todos os testes;
- remove antes da exportação Android;
- o workflow também remove logo após baixar o código;
- o arquivo foi adicionado ao .gitignore;
- o manifesto oficial continua sendo criado apenas no fim da publicação, depois de validar o APK.

Você não precisa localizar nem apagar esse arquivo manualmente. Substitua os arquivos do repositório por este pacote e faça um novo commit na main. Mesmo que o manifesto antigo ainda esteja rastreado no GitHub, a compilação o removerá do ambiente antes dos testes.

O download e a instalação das atualizações continuam automáticos dentro do app. O Android ainda exige a confirmação final "Atualizar" por segurança do sistema.
